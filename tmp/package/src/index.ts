/**
 * @package <%= name %>
 * @author <%= author %>
 * @description 包入口
 */

/**
 * 将变量a和变量b相加
 * @param {number} a 数字
 * @param {number} b 数字
 * @returns 变量a和变量b相加后的结果
 */
export function add(a: number, b: number) {
  return a + b
}
